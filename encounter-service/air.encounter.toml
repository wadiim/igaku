root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -buildvcs=false -o ./tmp/encounter-service ."
bin = "./tmp/encounter-service"
include_ext = ["go"]
include_dir = ["./", "../commons"]
exclude_dir = ["tmp", "bin", ".git"]
log = "air_encounter.log"
delay = 1000

[misc]
clean_on_exit = true
